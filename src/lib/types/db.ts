export type User = {
  id: string;
  username: string;
  provider: "credentials";
};

export type Event = {
  id: string;
  categoryName: string;
  location: string;
};

export type Food = {
  id: string;
  categoryName: string;
  location: string;
};
